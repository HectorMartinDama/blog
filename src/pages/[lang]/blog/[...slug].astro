---
import { getEntry } from "astro:content";

const { lang, slug }= Astro.params;

const page = await getEntry('blog', `${lang}/${slug}`);

console.log(lang, slug, 'hola')

if(!page){
    return Astro.redirect('/404');
}

const { Content }= await page.render();
---


<Content/>




